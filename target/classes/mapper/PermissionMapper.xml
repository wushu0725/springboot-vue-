<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="org.spring.springboot.dao.PermissionDao">
	<resultMap id="PermissionMap" type="org.spring.springboot.domain.Permission">
		<result column="id" property="id" />
		<result column="name" property="name" />
		<result column="desc" property="desc" />
		<result column="url" property="url" />
		<result column="pid" property="pid" />
	</resultMap>


	<select id="findAll"  resultType="org.spring.springboot.domain.Permission">

   		SELECT * from Sys_permission ;
	</select>

 	<select id="findByAdminUserId" parameterType="java.lang.Long" resultType="org.spring.springboot.domain.Permission">
      select p.*
        from User u
        LEFT JOIN role_user sru on u.id= sru.User_id
        LEFT JOIN Role r on sru.Role_id=r.id
        LEFT JOIN role_permission spr on spr.role_id=r.id
        LEFT JOIN permission p on p.id =spr.permission_id
        where u.id=#{userId}
 	</select>

</mapper>
